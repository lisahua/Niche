package seal.niche.lexicalAnalyser.tokenizer;

import seal.niche.lexicalAnalyser.model.IdentifierNode;

public interface INameExecutor {

//	public ArrayList<IdentifierNode> executeNames(
//			ArrayList<IdentifierNode> typeNodes) {
//		for (IdentifierNode tnode : typeNodes) {
//			executeSingleName(tnode);
//			for (IdentifierNode mnode : tnode.getChildren()) {
//				executeSingleName(mnode);
//				for (IdentifierNode vnode : mnode.getChildren()) {
//					executeSingleName(vnode);
//				}
//			}
//			for (IdentifierNode fnode : tnode.getChildren2())
//				executeSingleName(fnode);
//		}
//		return typeNodes;
//	}

//	public IdentifierNode executeSingleName(IdentifierNode node);
}
